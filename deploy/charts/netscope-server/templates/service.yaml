apiVersion: v1
kind: Service
metadata:
  name: {{ include "netscope-server.fullname" . }}
  labels:
    {{- include "netscope-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netscope-server.selectorLabels" . | nindent 4 }}
