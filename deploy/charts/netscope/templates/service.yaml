apiVersion: v1
kind: Service
metadata:
  name: {{ include "netscope.fullname" . }}
  labels:
    {{- include "netscope.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netscope.selectorLabels" . | nindent 4 }}
