apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netscope-server.fullname" . }}-test-connection"
  labels:
    {{- include "netscope-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netscope-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
