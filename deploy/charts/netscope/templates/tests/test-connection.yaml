apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "netscope.fullname" . }}-test-connection"
  labels:
    {{- include "netscope.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "netscope.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
